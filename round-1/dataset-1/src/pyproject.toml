[project]
name = "github-oss-dataset"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "ijson==3.3.0",
    "pandas==2.2.2",
    "requests==2.32.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
